# Lambda Layer Adapter (Phase 2)

Lambda deployment uses Node.js runtime + Typst Layer. Deploy with Pulumi; invoke via AWS SDK.

See [docs/lambda/](../../../docs/lambda/README.md) for deploy and usage.
